# Market Data
POLYGON_API_KEY=your_polygon_key_here
FINNHUB_API_KEY=your_finnhub_key_here
ALPHA_VANTAGE_KEY=your_alpha_vantage_key_here
NEWS_API_KEY=your_newsapi_key_here

# AI (use OpenRouter — preferred — or direct Anthropic)
OPENROUTER_API_KEY=sk-or-your_key_here
OPENROUTER_MODEL=openrouter/free
ANTHROPIC_API_KEY=

# Database
# Local dev: persisted under server/data/stocks.db (auto-created)
# DATABASE_URL=sqlite:///./server/data/stocks.db
#
# Railway: mount a volume at /data in the service settings, then either:
#   - leave DATABASE_URL unset (auto-uses /data/stocks.db on Railway), or
#   - set DATABASE_URL=sqlite:////data/stocks.db
DATABASE_URL=

# Server
# Comma-separated browser origins allowed to call the API.
# After deploying the frontend, add your Vercel URL, e.g.:
# ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://vectra.vercel.app
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
# Vercel preview deployments (disable with empty string if undesired)
ALLOWED_ORIGIN_REGEX=https://.*\.vercel\.app
CACHE_TTL_SECONDS=60
PORT=8000
